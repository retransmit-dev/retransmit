/**
 * Shared by the emails page (server, for prefetching) and its client
 * components, so the first query on the browser matches the one started on
 * the server.
 */
export const EMAILS_PAGE_SIZE = 25;
export const EMAILS_DEFAULT_DAYS = 14;
