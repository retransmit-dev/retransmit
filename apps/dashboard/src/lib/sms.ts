/**
 * Shared by the SMS page (server, for prefetching) and its client
 * components, so the first query on the browser matches the one started on
 * the server.
 */
export const SMS_PAGE_SIZE = 25;
export const SMS_DEFAULT_DAYS = 14;
