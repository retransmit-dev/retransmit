/**
 * Shared by the suppressions page (server, for prefetching) and its client
 * components, so the first query on the browser matches the one started on
 * the server.
 */
export const SUPPRESSIONS_PAGE_SIZE = 25;
